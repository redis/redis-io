!!! 5

%html
  %head
    %meta(charset="utf-8")
    %link(rel="stylesheet" href="/styles.css?#{File.mtime("views/styles.sass").to_i}" type="text/css")

    %title
      = [@title, "Redis"].compact.join(" – ")

  %body

    %header
      - if user
        %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(user.email)}.jpg?s=20" width="20" height="20")

    = content
