!!! 5

%html
  %head
    %meta(charset="utf-8")
    %link(rel="stylesheet" href="/styles.css?#{File.mtime("views/styles.sass").to_i}" type="text/css")

    %title
      = [@title, "Redis"].compact.join(" – ")

  %body

    %header
      %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(User[session[:user]].email)}.jpg?s=20" width="20" height="20")

    = content
