%section#commands
  %nav
    %a(href="#") All

    - Reference::GROUPS.each do |name, description|
      %a(href="##{name}")= description
    %input(type="text" id="filter")
  %ul
    - @commands.each do |command|
      %li(class="#{command.name.downcase}" data-group="#{command.command["group"]}")
        %span.command
          %a(href="/commands/#{command.to_param}")= command.name

          %span.args
            - command.arguments.each do |argument|
              = argument

        %span.summary= command.command["summary"]
